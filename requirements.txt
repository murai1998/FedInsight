# FedInsight - RAG over Federal Reserve Documents
#
# Hugging Face Spaces (CPU tier): pull the CPU-only torch build instead of the
# default CUDA wheel. Keeps the image small and avoids build-size/time limits.
# Harmless locally — pip falls back to PyPI for everything else.
--extra-index-url https://download.pytorch.org/whl/cpu

# Core
requests>=2.31.0
beautifulsoup4>=4.12.0
tqdm>=4.66.0
python-dotenv>=1.0.0

# PDF parsing
# PyMuPDF is the primary engine: correctly handles the 2-column FOMC layout.
pymupdf>=1.24.0
pdfplumber>=0.11.0          # optional fallback extractor

# Data processing
numpy>=1.26.0

# Embeddings
sentence-transformers>=3.0.0
# torch powers sentence-transformers. The embedder auto-selects the fastest
# device: CUDA (NVIDIA) -> MPS (Apple Silicon) -> CPU.
#   * Apple Silicon Mac: the default macOS wheel already includes MPS (Metal)
#     acceleration — no special install needed.
#   * NVIDIA GPU: install the matching CUDA build instead of the CPU wheel,
#     e.g. for Blackwell (RTX 50-series):
#       pip install --index-url https://download.pytorch.org/whl/cu128 torch
torch>=2.2.0

# Vector DB
# Default backend for local/testing: ChromaDB (no server required)
chromadb>=0.5.0
# Production backend: pgvector (PostgreSQL) - requires a running Postgres
psycopg[binary]>=3.2.0
pgvector>=0.3.0

# Chunking: only the lightweight text-splitters package is used
# (RecursiveCharacterTextSplitter in src/chunker/text_chunker.py).
langchain-text-splitters>=0.2.0

# UI
streamlit>=1.35.0

# Dev / logging
loguru>=0.7.0